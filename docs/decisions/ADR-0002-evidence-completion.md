# ADR-0002：无证据不得完成

- 状态：Accepted
- 日期：2026-08-06

## 决定

Task 从任何状态进入 `completed` 时，必须满足：

1. 至少引用一个 Evidence；
2. Evidence 的验证状态为 `passed` 或 `accepted`；
3. 完成标准没有未满足项；
4. 需要人工批准时，审批已经记录。

## 原因

生成、提交、局部测试和“Agent 认为完成”不能证明现实结果。该规则用于抑制假完成，并为跨 Agent 恢复提供可靠事实。
