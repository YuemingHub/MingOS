{
  "name": "mingos",
  "version": "0.1.0",
  "private": true,
  "description": "MingOS cross-space protocols and reference kernel",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate:examples": "node packages/kernel/src/validate.mjs examples",
    "check": "npm test && npm run validate:examples",
    "ming": "node packages/cli/bin/ming.mjs"
  },
  "bin": {
    "ming": "./packages/cli/bin/ming.mjs"
  }
}
