# 治理边界

MingOS 的治理不再采用“Foundation → MingOS → Space → Tool”的单向层级。

`foundation/` 中保存的是**原则 / 校准文本**，不是更高一级的命令源。它们保持原本的 Candidate / Draft 状态，只能帮助校准判断，不能覆盖人的主体性、PRK 中的个人现实、产品仓库的当前事实或运行时证据。

## 当前权威域

| 权威域 | 当前拥有者 | 能决定什么 | 不能决定什么 |
|---|---|---|---|
| 人的主体性 | 当前的人 | 同意、拒绝、暂停、修正、撤回、最终选择 | 不能被历史画像、模型推断或原则文本替代 |
| 个人现实 | PRK + Owner 授权边界 | 原始证据、个人现实连续性、可修正的派生理解 | 不能自动变成产品事实或对他人的事实 |
| 产品当前事实 | 对应 Project Repo + runtime | 当前实现、部署、产品状态、真实运行结果 | 不能自动升级成 MingOS primitive 或 Foundation principle |
| 共享语义 | MingOS | 被多个独立真实表面重复证明、确有必要的跨空间语义 | 不主动从上层设计产品，不为体系完整而扩张 |
| 原则 / 校准 | `foundation/` | 提供生命、安全、尊严、主体性、修正权等候选原则视角 | 不直接发号施令，不拥有 conformance/governance runtime |
| Adapter / Tool | 外部 Agent / MCP / API / DB / UI 等 | 实现可替换能力 | 不获得人的最终决定权或现实真源地位 |

这些权威域之间**不能静默覆盖彼此**。

现实、反例、受影响的人和新的证据可以推动任何原则、共享语义或产品判断被修正。需要跨域升级时，必须显式说明来源、证据和授权，不能因为“上层文件这样写”就自动成立。

## MingOS 的提升纪律

只有当同一个语义缺口在多个独立真实表面反复出现，并且：

- 项目本地修复不足；
- PRK / Agent Space / 现有共享语义不能表达；
- 外部成熟能力也不能合理承接；
- 抽象不会创造第二份现实权威；

才允许进入 MingOS 共享语义层。

产品实现不得自动升级为 MingOS Core primitive。

## 人的最终权利

任何空间至少保留以下权利：

- 知道系统保存了什么；
- 区分事实与 AI 推断；
- 确认、修正、否认、撤回；
- 限制或取消 Agent 授权；
- 要求解释执行依据；
- 查看完成证据；
- 导出并迁移空间资产；
- 在重大风险与不可逆操作前取得最终决定权。
